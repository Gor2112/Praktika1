#include <iostream>
#include <list>
using namespace std;

int main()
{
    list<int> listData;

    listData.push_back(100);
    listData.push_back(200);
    listData.push_back(300);
    listData.push_back(400);

    return 0;
}
